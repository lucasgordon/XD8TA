<div class="container-flouid">
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">
          <h3>XD8TA</h3>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
          <% if current_user %>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to "My Profile", user_path(current_user), class: "nav-link active" %>
              </li>
              <li class="nav-item">
                <%= link_to "My Posts", posts_user_path(current_user), class: "nav-link active" %>
              </li>
              <li>
                <% if current_user %>
                  <%= current_user.name %>
                <% end %>
              </li>
    
            </ul>
          <% end %>
          <form class="">
            <%= link_to "Sign In", sign_in_path %>
            <%= link_to "Sign Up", sign_up_path %>
            <%= link_to "Sign Out", sign_out_path, method: :delete %>
          </form>
  
        </div>
      </div>
    </nav>
  </div>